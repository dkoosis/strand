## lane: WildSandpiper

→ next: rebuild `web/mockup/v1.html` as the **strand** — sized story-cards (size=open work, heat=child recency) + north-star masthead, real trixi epic roll-ups; flat table → drill-in. Final picture-review pending, then execute.

✓ synthesis reframe → spec §1a (vault+repo synced); V1 table spike built then pivoted away from it.

‡ strand-5ri.1 still in_progress (spike not signed off). `git stash list` holds pre-session boot.md/.beads/.claude.

~ dk in big-picture-shaping flow; steers by reacting to visuals — lead with the mockup.

## lane: DimTrout

→ next: dk merges PR #30 then #29 → close str-6k0.6.2/.6.3 + the .6 epic → dispatch str-6k0.5 (clarity+a11y, last pass over the finished redesign).

✓ drawer trio shipped: .6.4 merged (#28); .6.2 (#30) + .6.3 (#29) PRs open. .4 + .6.4 beads closed.

‡ #29↔#30 collide in server.go / server_test.go stubBD / write.go — merge #30 first (pointer-sig, wider blast); #29 also vs merged #28 in partials.html drawer order. str-y1m lint bead open.

~ dk: verify-before-assert — RTFM beat trusting the survey agent.
