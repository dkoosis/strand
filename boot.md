## lane: WildSandpiper

→ next: rebuild `web/mockup/v1.html` as the **forest** — sized story-cards (size=open work, heat=child recency) + north-star masthead, real trixi epic roll-ups; flat table → drill-in. Final picture-review pending, then execute.

✓ forest reframe → spec §1a (vault+repo synced); V1 table spike built then pivoted away from it.

‡ strand-5ri.1 still in_progress (spike not signed off). `git stash list` holds pre-session boot.md/.beads/.claude.

~ dk in big-picture-shaping flow; steers by reacting to visuals — lead with the mockup.
