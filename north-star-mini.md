strand renders synthesis, not the catalog — where the mass, bugs, and stuck work sit, so a human can orient → decide → shape.

The human shapes the top (epics, stories, priority); agents own below the story line.
